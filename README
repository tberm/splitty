# Splitty

This is a WIP experiment in building a mobile app and backend entirely with Claude.

The app is a shared expense sharing app with receipt-scanning feature for easy
handling of itemised expenses where different participants need to assign
themselves different part of a bill.

The mobile app is built with Expo and the backend with FastAPI over a PostgreSQL DB.